import { StyleSheet, Text, View } from "react-native";
import React from "react";

const LiveSession = () => {
  return (
    <View>
      <Text>LiveSession</Text>
    </View>
  );
};

export default LiveSession;

const styles = StyleSheet.create({});
